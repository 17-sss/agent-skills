interface:
  display_name: "Audio Asset Generator (Experimental)"
  short_description: "Generate and integrate production audio assets safely"
  default_prompt: "Use $audio-asset-generator to inspect this product's audio needs, prefer zero-cost valid generation routes, create and integrate only the assets supported by available capabilities, provide category-specific prompts for the rest, enforce voice consent and disclosure, and separate file validation from listening evidence without silently spending money."
policy:
  allow_implicit_invocation: false
