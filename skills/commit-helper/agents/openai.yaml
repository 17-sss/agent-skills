interface:
  display_name: "Commit Helper"
  short_description: "Draft repo-specific commit messages across common style families"
  default_prompt: "Use $commit-helper's single draft path to inspect repo rules, history, and staged changes once, then draft or create the authorized commit in the right style family."
