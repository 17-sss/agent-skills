interface:
  display_name: "Codex · Completion Loop"
  short_description: "Freeze scope and finish with focused evidence"
  default_prompt: "Use $completion-loop to freeze the goal scope and deployment target, execute risk-tiered checks, focus rereview on accepted blockers, and finish with concise evidence."
policy:
  allow_implicit_invocation: false
