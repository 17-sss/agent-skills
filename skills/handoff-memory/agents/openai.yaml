interface:
  display_name: "Handoff Memory"
  short_description: "Keep reusable repo and workspace handoff memory"
  default_prompt: "Use $handoff-memory to resolve or refresh the shared repo-local or workspace-local HANDOFF for this project."
