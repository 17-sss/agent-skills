interface:
  display_name: "Codex · Review Gate"
  short_description: "Strict pre-PR and pre-merge review gate"
  default_prompt: "Use $review-gate as the final pre-PR or pre-merge gate for this high-risk change: capture a sensitivity-screened snapshot, run sandbox-isolated correctness and architecture lanes, then return a deterministic readiness verdict."
policy:
  allow_implicit_invocation: false
