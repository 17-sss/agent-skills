interface:
  display_name: "Spec Interview"
  short_description: "Clarify ambiguous work before planning or coding"
  default_prompt: "Use $spec-interview to clarify this idea one decision at a time, present bounded decisions as concise choices with a custom-answer escape hatch, keep repository inspection non-mutating, and produce an execution-ready requirements specification."
policy:
  allow_implicit_invocation: false
