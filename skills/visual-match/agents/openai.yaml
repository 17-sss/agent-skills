interface:
  display_name: "Visual Match"
  short_description: "Measure and match an approved visual reference"
  default_prompt: "Use $visual-match to implement this approved visual reference, compare equivalent renders from major landmarks through repeated grouping to local polish, require a native paired-image verdict before each coherent repair batch, use bundled PNG hotspots only as secondary evidence, and confirm the first validated pass with a fresh raw-pair audit before finishing."
policy:
  allow_implicit_invocation: false
